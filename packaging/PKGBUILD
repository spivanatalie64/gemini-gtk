# Maintainer: Natalie Spiva <natalie@example.com>
pkgname=ai-box
pkgver=1.0.0
pkgrel=1
pkgdesc="AI-Box - Premium multi-platform AI client with local Ollama support"
arch=('x86_64')
url="https://github.com/spivanatalie64/gemini-gtk"
license=('MIT')
depends=('electron' 'nodejs' 'npm')
optdepends=('ollama: Local AI model support')
source=("$pkgname-$pkgver.tar.gz::https://github.com/spivanatalie64/gemini-gtk/archive/main.tar.gz")
sha256sums=('c23d3430c84c04ad4ed11f1001ec9160954e1e95c0f70d3c7c9db04062d6a4c7')

build() {
  cd "$srcdir/gemini-gtk-main"
  npm install
}

package() {
  cd "$srcdir/gemini-gtk-main"
  
  echo "DEBUG: Listing files in current directory"
  ls -la

  # Install application files
  install -dm755 "$pkgdir/usr/lib/$pkgname"
  cp -r package.json "$pkgdir/usr/lib/$pkgname/"
  cp -r node_modules "$pkgdir/usr/lib/$pkgname/"
  
  # Install legacy files (documentation)
  install -dm755 "$pkgdir/usr/share/doc/$pkgname"
  cp -r legacy_gtk "$pkgdir/usr/share/doc/$pkgname/"
  
  # Create launcher script
  install -dm755 "$pkgdir/usr/bin"
  cat > "$pkgdir/usr/bin/$pkgname" << EOF
#!/bin/bash
cd /usr/lib/$pkgname
electron .
EOF
  chmod +x "$pkgdir/usr/bin/$pkgname"
  
  # Install desktop file
  install -Dm644 /dev/stdin "$pkgdir/usr/share/applications/$pkgname.desktop" << EOF
[Desktop Entry]
Name=AI-Box
Comment=Premium multi-platform AI client with local Ollama support
Exec=$pkgname
Icon=utilities-terminal
Terminal=false
Type=Application
Categories=Utility;Network;
EOF
}
